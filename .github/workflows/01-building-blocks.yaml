name: 01-building-blocks.yaml

on:
    #push:
        # branches:
        #     - master
    workflow_dispatch: {}

jobs:
    echo-hello:
      runs-on: ubuntu-latest
      steps:
          - name: say-hello
            run: 
                echo "'Hello World'"

    echo-goodbye:
      runs-on: ubuntu-latest
      steps: 
        - name: failed step
          run: |
                echo "'Goodbye World'"
                echo "This step will fail"
                exit 1
        - name: say-goodbye
          run: echo 'Goodbye'